/** 
 * This component doesn't contain any piece of code that could have bugs or issues;
 * It only contains HTML tags and displays a simple text when it's rendered.
 * Given that there's no interactivity (e.g., buttons, forms, clickable elements, etc.) in this component or any kind of computed properties or methods to work with;
 * There's no need for doing unit testing on this specific component.
 */
